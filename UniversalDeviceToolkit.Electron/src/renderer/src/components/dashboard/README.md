# Dashboard sensor panels

Live CPU / GPU / battery / memory charts and gauges.

Feature cards (power mode, GPU extras, God Mode) live in `../dashboard-parity/`. That folder is WPF dashboard-control layout parity, not a second app. Do not merge the two trees.
