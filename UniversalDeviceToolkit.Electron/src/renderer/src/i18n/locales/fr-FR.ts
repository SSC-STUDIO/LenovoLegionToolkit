/**
 * French locale. The full translation set ships in ./fr (ported from the WPF
 * Resource.fr.resx); this module keeps the canonical `fr` entry point used by
 * the i18n registry (index.ts).
 */
export { default } from './fr'
