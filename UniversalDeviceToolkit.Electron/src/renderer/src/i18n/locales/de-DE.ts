/**
 * German locale. The full translation set ships in ./de (ported from the WPF
 * Resource.de.resx); this module keeps the canonical `de` entry point used by
 * the i18n registry (index.ts).
 */
export { default } from './de'
