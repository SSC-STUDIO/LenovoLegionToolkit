namespace UniversalDeviceToolkit.Electron.Launcher;

/// <summary>
/// No-op stub. The Electron client is a Node.js project (package.json +
/// electron-vite); this project exists only so Visual Studio can host it as
/// a startup project. Use the "Electron (npm run dev)" launch profile (F5)
/// or run `npm run dev` in the UniversalDeviceToolkit.Electron directory.
/// </summary>
public static class Program
{
    public static void Main()
    {
        Console.WriteLine("Universal Device Toolkit — Electron launcher stub.");
        Console.WriteLine("Start the client with `npm run dev` in UniversalDeviceToolkit.Electron,");
        Console.WriteLine("or press F5 in Visual Studio (Electron launch profile).");
    }
}
