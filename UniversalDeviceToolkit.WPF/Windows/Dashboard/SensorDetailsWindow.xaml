<local:BaseWindow
    x:Class="UniversalDeviceToolkit.WPF.Windows.Dashboard.SensorDetailsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dashboard="clr-namespace:UniversalDeviceToolkit.WPF.Controls.Dashboard"
    xmlns:local="clr-namespace:UniversalDeviceToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:UniversalDeviceToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:UniversalDeviceToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="1160"
    Height="720"
    MinWidth="980"
    MinHeight="560"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    Loaded="SensorDetailsWindow_Loaded"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">
    <AutomationProperties.AutomationId>SensorDetailsWindow</AutomationProperties.AutomationId>

    <Grid Background="{DynamicResource AppNavigationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            Grid.Row="0"
            CanMaximize="false"
            ShowMaximize="false"
            ShowMinimize="false" />

        <Grid
            Grid.Row="0"
            Height="24"
            Margin="12,8,12,8">
            <TextBlock
                x:Name="_titleText"
                VerticalAlignment="Center"
                Style="{StaticResource AppCaptionTextBlockStyle}"
                IsHitTestVisible="False" />
        </Grid>

        <wpfui:DynamicScrollViewer
            Grid.Row="1"
            Margin="12,0,12,12"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <dashboard:SensorsControl x:Name="_sensors" />
        </wpfui:DynamicScrollViewer>

        <Grid Grid.Row="2" Margin="12,0,12,12">
            <wpfui:Button
                MinWidth="100"
                HorizontalAlignment="Right"
                Click="CloseButton_Click"
                Content="{x:Static resources:Resource.Close}" />
        </Grid>
    </Grid>
</local:BaseWindow>
