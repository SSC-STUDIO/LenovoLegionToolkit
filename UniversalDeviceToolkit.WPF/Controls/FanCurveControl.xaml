<UserControl
    x:Class="UniversalDeviceToolkit.WPF.Controls.FanCurveControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
    xmlns:resources="clr-namespace:UniversalDeviceToolkit.WPF.Resources"
    FlowDirection="LeftToRight">
    <UserControl.Resources>
        <Style x:Key="FanCurveSliderStyle" TargetType="Slider">
            <Setter Property="Width" Value="22" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid Background="Transparent">
                            <!-- Max at top so the curve rises with temperature (matches 100%…20% labels). -->
                            <primitives:Track
                                x:Name="PART_Track"
                                Margin="0,10"
                                IsDirectionReversed="False">
                                <primitives:Track.DecreaseRepeatButton>
                                    <RepeatButton
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Command="Slider.DecreaseLarge" />
                                </primitives:Track.DecreaseRepeatButton>
                                <primitives:Track.IncreaseRepeatButton>
                                    <RepeatButton
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Command="Slider.IncreaseLarge" />
                                </primitives:Track.IncreaseRepeatButton>
                                <primitives:Track.Thumb>
                                    <Thumb Width="18" Height="18">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse
                                                    x:Name="ThumbSurface"
                                                    Fill="{DynamicResource ChartUtilizationBrush}"
                                                    Stroke="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                                                    StrokeThickness="2" />
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbSurface" Property="Stroke" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbSurface" Property="StrokeThickness" Value="2.5" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </primitives:Track.Thumb>
                            </primitives:Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="0,8,0,8">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid
            Grid.Row="0"
            Grid.Column="0"
            Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <!-- Y-axis = fan speed % (not temperature). Top was wrongly bound to TemperatureMax. -->
            <Label
                Grid.Row="0"
                HorizontalAlignment="Right"
                VerticalContentAlignment="Center"
                Content="{x:Static resources:Resource.FanCurveControl_FanSpeedMax}"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <Label
                Grid.Row="2"
                HorizontalAlignment="Right"
                VerticalContentAlignment="Center"
                Content="80%"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <Label
                Grid.Row="4"
                HorizontalAlignment="Right"
                VerticalContentAlignment="Center"
                Content="60%"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <Label
                Grid.Row="6"
                HorizontalAlignment="Right"
                VerticalContentAlignment="Center"
                Content="40%"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <Label
                Grid.Row="8"
                HorizontalAlignment="Right"
                VerticalContentAlignment="Center"
                Content="20%"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <Label
                Grid.Row="10"
                HorizontalAlignment="Right"
                VerticalContentAlignment="Center"
                Content="{x:Static resources:Resource.FanCurveControl_FanSpeed}"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        </Grid>

        <Border
            Grid.Row="0"
            Grid.Column="1"
            BorderBrush="{DynamicResource TextFillColorDisabledBrush}"
            BorderThickness="1,0,0,1" />

        <Canvas
            x:Name="_canvas"
            Grid.Row="0"
            Grid.Column="1" />

        <Grid
            x:Name="_slidersGrid"
            Grid.Row="0"
            Grid.Column="1"
            Height="200">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
        </Grid>

        <Grid
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label
                Grid.Row="1"
                HorizontalAlignment="Right"
                VerticalContentAlignment="Center"
                Content="{x:Static resources:Resource.FanCurveControl_TemperatureMax}"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        </Grid>

    </Grid>
</UserControl>
