<UserControl
    x:Class="UniversalDeviceToolkit.WPF.Controls.Dashboard.SensorsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:UniversalDeviceToolkit.WPF.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:UniversalDeviceToolkit.WPF.Resources"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="1200"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock" BasedOn="{StaticResource AppSectionTitleTextBlockStyle}">
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
        <Style x:Key="StatLabelStyle" TargetType="Label">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        </Style>
        <Style x:Key="StatValueStyle" TargetType="Label">
            <Setter Property="HorizontalContentAlignment" Value="Right" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        </Style>
        <Style x:Key="DetailToggleButtonStyle" TargetType="wpfui:Button">
            <Setter Property="Appearance" Value="Transparent" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="0,16,0,0" />
        </Style>
        <Style x:Key="SensorChartCardStyle" TargetType="Border">
            <Setter Property="MinHeight" Value="154" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style x:Key="SensorChartValueTextStyle" TargetType="TextBlock" BasedOn="{StaticResource AppSmallBodyTextBlockStyle}">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style x:Key="SensorChartCaptionTextStyle" TargetType="TextBlock" BasedOn="{StaticResource AppCaptionTextBlockStyle}">
            <Setter Property="Opacity" Value="0.78" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style x:Key="SensorChartLegendTextStyle" TargetType="TextBlock" BasedOn="{StaticResource AppCaptionTextBlockStyle}">
            <Setter Property="Margin" Value="4,0,12,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
    </UserControl.Resources>

    <wpfui:CardControl
        AutomationProperties.AutomationId="DashboardSensorsCard"
        Background="{DynamicResource AppSurfaceCardBrush}"
        Focusable="False"
        PreviewMouseLeftButtonDown="CardControl_PreviewMouseLeftButtonDown">
        <wpfui:CardControl.Header>
            <StackPanel Margin="16,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel x:Name="_cpuSection" Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16" HorizontalAlignment="Left">
                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_CPU_Title}" Style="{StaticResource SectionTitleStyle}" Margin="0" />
                            <TextBlock x:Name="_cpuModelName" Text="" Margin="8,0,0,0" VerticalAlignment="Bottom" Visibility="Collapsed" Style="{StaticResource AppCaptionTextBlockStyle}" TextTrimming="CharacterEllipsis" MaxWidth="150" />
                        </StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="88" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Label Grid.Row="0" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Utilization_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_cpuUtilizationBar" Grid.Row="0" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_cpuUtilizationLabel" Grid.Row="0" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="1" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_CoreClock_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_cpuCoreClockBar" Grid.Row="1" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_cpuCoreClockLabel" Grid.Row="1" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="2" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_cpuTemperatureBar" Grid.Row="2" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_cpuTemperatureLabel" Grid.Row="2" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="3" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Fan_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_cpuFanSpeedBar" Grid.Row="3" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_cpuFanSpeedLabel" Grid.Row="3" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />
                        </Grid>

                        <StackPanel x:Name="_cpuDetailsPanel" AutomationProperties.AutomationId="DashboardSensorsCpuDetailsPanel" Visibility="Collapsed" Margin="0,16,0,0">
                            <Border x:Name="_cpuChartPanel" AutomationProperties.AutomationId="DashboardSensorsCpuChart" Style="{StaticResource SensorChartCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Canvas x:Name="_cpuChartCanvas" Grid.Row="0" Height="76" ClipToBounds="True">
                                        <Polygon x:Name="_cpuUtilizationArea" Fill="{DynamicResource SystemAccentColorBrush}" Opacity="0.14" />
                                        <Polygon x:Name="_cpuClockArea" Fill="{DynamicResource SystemFillColorSuccessBrush}" Opacity="0.10" />
                                        <Polygon x:Name="_cpuTemperatureArea" Fill="{DynamicResource SystemFillColorCautionBrush}" Opacity="0.12" />
                                        <Line X1="0" Y1="14" X2="420" Y2="14" Stroke="{DynamicResource CardStrokeColorDefaultBrush}" StrokeThickness="1" Opacity="0.55" />
                                        <Line X1="0" Y1="38" X2="420" Y2="38" Stroke="{DynamicResource CardStrokeColorDefaultBrush}" StrokeThickness="1" Opacity="0.35" />
                                        <Line X1="0" Y1="62" X2="420" Y2="62" Stroke="{DynamicResource CardStrokeColorDefaultBrush}" StrokeThickness="1" Opacity="0.55" />
                                        <Polyline x:Name="_cpuUtilizationSparkline" Stroke="{DynamicResource SystemAccentColorBrush}" StrokeThickness="2" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                        <Polyline x:Name="_cpuClockSparkline" Stroke="{DynamicResource SystemFillColorSuccessBrush}" StrokeThickness="2" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                        <Polyline x:Name="_cpuTemperatureSparkline" Stroke="{DynamicResource SystemFillColorCautionBrush}" StrokeThickness="2" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                    </Canvas>
                                    <UniformGrid Grid.Row="1" Columns="3" Margin="0,8,0,0">
                                        <StackPanel Margin="0,0,8,0">
                                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_Utilization_Title}" Style="{StaticResource SensorChartCaptionTextStyle}" />
                                            <TextBlock x:Name="_cpuChartUtilizationText" Text="-" Style="{StaticResource SensorChartValueTextStyle}" />
                                        </StackPanel>
                                        <StackPanel Margin="4,0,4,0">
                                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_CoreClock_Title}" Style="{StaticResource SensorChartCaptionTextStyle}" />
                                            <TextBlock x:Name="_cpuChartClockText" Text="-" Style="{StaticResource SensorChartValueTextStyle}" />
                                        </StackPanel>
                                        <StackPanel Margin="8,0,0,0">
                                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource SensorChartCaptionTextStyle}" />
                                            <TextBlock x:Name="_cpuChartTemperatureText" Text="-" Style="{StaticResource SensorChartValueTextStyle}" />
                                        </StackPanel>
                                    </UniformGrid>
                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                                        <Rectangle Width="8" Height="8" RadiusX="4" RadiusY="4" Fill="{DynamicResource SystemAccentColorBrush}" VerticalAlignment="Center" />
                                        <TextBlock Text="{x:Static resources:Resource.SensorsControl_Utilization_Title}" Style="{StaticResource SensorChartLegendTextStyle}" />
                                        <Rectangle Width="8" Height="8" RadiusX="4" RadiusY="4" Fill="{DynamicResource SystemFillColorSuccessBrush}" VerticalAlignment="Center" />
                                        <TextBlock Text="{x:Static resources:Resource.SensorsControl_CoreClock_Title}" Style="{StaticResource SensorChartLegendTextStyle}" />
                                        <Rectangle Width="8" Height="8" RadiusX="4" RadiusY="4" Fill="{DynamicResource SystemFillColorCautionBrush}" VerticalAlignment="Center" />
                                        <TextBlock Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource SensorChartLegendTextStyle}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                    <TextBlock x:Name="_cpuWattageTitle" Text="{x:Static resources:Resource.SensorsControl_PowerUsage}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuWattage" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_cpuVoltageTitle" Text="{x:Static resources:Resource.SensorsControl_Voltage}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuVoltage" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_cpuPCoreClockTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuPCoreClock" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_cpuECoreClockTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuECoreClock" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_cpuMemoryUsageTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuMemoryUsage" Text="" Margin="0,0,0,8" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                    <TextBlock x:Name="_cpuTempRangeTitle" Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuTempRange" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_cpuVoltageRangeTitle" Text="{x:Static resources:Resource.SensorsControl_VoltageRange}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuVoltageRange" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_cpuMemoryTemperatureTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuMemoryTemperature" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_cpuMotherboardTemperatureTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuMotherboardTemperature" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_cpuSsdTemperatureTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_cpuSsdTemperature" Text="" Margin="0,0,0,8" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </StackPanel>

                    <Border x:Name="_cpuGpuSeparatorLeft" Grid.Column="1" Width="1" Background="{DynamicResource CardStrokeColorDefaultBrush}" Margin="16,0" />

                    <StackPanel x:Name="_batterySectionColumn" Grid.Column="2">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16" HorizontalAlignment="Left">
                            <TextBlock Text="{x:Static resources:Resource.BatteryPage_Title}" Style="{StaticResource SectionTitleStyle}" Margin="0" />
                            <TextBlock x:Name="_batteryModelName" Text="" Margin="8,0,0,0" VerticalAlignment="Bottom" Visibility="Collapsed" Style="{StaticResource AppCaptionTextBlockStyle}" TextTrimming="CharacterEllipsis" MaxWidth="150" />
                        </StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="88" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Label Grid.Row="0" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Charge}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_batteryPercentageBar" Grid.Row="0" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_batteryPercentageLabel" Grid.Row="0" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="1" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Health}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_batteryHealthBar" Grid.Row="1" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_batteryHealthText" Grid.Row="1" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="2" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_batteryTemperatureBar" Grid.Row="2" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_batteryTempText" Grid.Row="2" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="3" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Rate}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_batteryRateBar" Grid.Row="3" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_batteryRateText" Grid.Row="3" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />
                        </Grid>

                        <StackPanel Margin="0,8,0,0">
                            <TextBlock x:Name="_batteryStatusLabel" Text="" Style="{StaticResource AppCaptionTextBlockStyle}" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Visibility="Collapsed" />
                            <TextBlock x:Name="_lowBatteryWarning" Text="{x:Static resources:Resource.BatteryPage_LowLevel}" Foreground="{DynamicResource SystemFillColorCautionBrush}" Visibility="Collapsed" HorizontalAlignment="Center" Style="{StaticResource AppCaptionTextBlockStyle}" />
                            <TextBlock x:Name="_lowWattageWarning" Text="{x:Static resources:Resource.BatteryPage_LowWattageChargerConnected}" Foreground="{DynamicResource SystemFillColorCautionBrush}" Visibility="Collapsed" HorizontalAlignment="Center" Style="{StaticResource AppCaptionTextBlockStyle}" />
                            <wpfui:SymbolIcon x:Name="_batteryIcon" Symbol="Battery024" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,0" Visibility="Collapsed" />
                        </StackPanel>

                        <StackPanel x:Name="_batteryDetailsPanel" AutomationProperties.AutomationId="DashboardSensorsBatteryDetailsPanel" Visibility="Collapsed" Margin="0,16,0,0">
                            <Border x:Name="_batteryCapacityPanel" AutomationProperties.AutomationId="DashboardSensorsBatteryCapacityChart" Style="{StaticResource SensorChartCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static resources:Resource.SensorsControl_Capacity}" Style="{StaticResource AppCaptionTextBlockStyle}" VerticalAlignment="Center" />
                                    <ProgressBar x:Name="_batteryCapBar" Grid.Row="0" Grid.Column="1" Margin="10,0,8,0" Height="8" Minimum="0" Maximum="100">
                                        <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                                    </ProgressBar>
                                    <TextBlock x:Name="_batteryCapChartText" Grid.Row="0" Grid.Column="2" Text="-" MinWidth="48" TextAlignment="Right" Style="{StaticResource AppCaptionTextBlockStyle}" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static resources:Resource.SensorsControl_FullCapacity}" Margin="0,8,0,0" Style="{StaticResource AppCaptionTextBlockStyle}" VerticalAlignment="Center" />
                                    <ProgressBar x:Name="_batteryFullCapBar" Grid.Row="1" Grid.Column="1" Margin="10,8,8,0" Height="8" Minimum="0" Maximum="100">
                                        <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                                    </ProgressBar>
                                    <TextBlock x:Name="_batteryFullCapChartText" Grid.Row="1" Grid.Column="2" Text="-" Margin="0,8,0,0" MinWidth="48" TextAlignment="Right" Style="{StaticResource AppCaptionTextBlockStyle}" />

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="{x:Static resources:Resource.SensorsControl_Health}" Margin="0,8,0,0" Style="{StaticResource AppCaptionTextBlockStyle}" VerticalAlignment="Center" />
                                    <ProgressBar x:Name="_batteryHealthDetailBar" Grid.Row="2" Grid.Column="1" Margin="10,8,8,0" Height="8" Minimum="0" Maximum="100">
                                        <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                                    </ProgressBar>
                                    <TextBlock x:Name="_batteryHealthChartText" Grid.Row="2" Grid.Column="2" Text="-" Margin="0,8,0,0" MinWidth="48" TextAlignment="Right" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                </Grid>
                            </Border>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_RateRange}" Style="{StaticResource AppSmallBodyTextBlockStyle}" />
                                    <TextBlock x:Name="_batteryRateRange" Text="-" />
                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_Cycles}" Style="{StaticResource AppSmallBodyTextBlockStyle}" Margin="0,4,0,0" />
                                    <TextBlock x:Name="_batteryCycles" Text="-" />
                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_Date}" Style="{StaticResource AppSmallBodyTextBlockStyle}" Margin="0,4,0,0" />
                                    <TextBlock x:Name="_batteryDate" Text="-" />
                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource AppSmallBodyTextBlockStyle}" Margin="0,4,0,0" />
                                    <TextBlock x:Name="_batteryTemperature" Text="-" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_Capacity}" Style="{StaticResource AppSmallBodyTextBlockStyle}" />
                                    <TextBlock x:Name="_batteryCap" Text="-" />
                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_FullCapacity}" Style="{StaticResource AppSmallBodyTextBlockStyle}" Margin="0,4,0,0" />
                                    <TextBlock x:Name="_batteryFullCap" Text="-" />
                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_DesignCapacity}" Style="{StaticResource AppSmallBodyTextBlockStyle}" Margin="0,4,0,0" />
                                    <TextBlock x:Name="_batteryDesignCap" Text="-" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </StackPanel>

                    <Border x:Name="_cpuGpuSeparatorRight" Grid.Column="3" Width="1" Background="{DynamicResource CardStrokeColorDefaultBrush}" Margin="16,0" />

                    <StackPanel x:Name="_gpuSection" Grid.Column="4">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16" HorizontalAlignment="Left">
                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_GPU_Title}" Style="{StaticResource SectionTitleStyle}" Margin="0" />
                            <TextBlock x:Name="_gpuModelName" Text="" Margin="8,0,0,0" VerticalAlignment="Bottom" Visibility="Collapsed" Style="{StaticResource AppCaptionTextBlockStyle}" TextTrimming="CharacterEllipsis" MaxWidth="150" />
                        </StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="88" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Label Grid.Row="0" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Utilization_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_gpuUtilizationBar" Grid.Row="0" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_gpuUtilizationLabel" Grid.Row="0" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="1" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_CoreClock_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_gpuCoreClockBar" Grid.Row="1" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_gpuCoreClockLabel" Grid.Row="1" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="2" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_gpuTemperatureBar" Grid.Row="2" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_gpuTemperatureLabel" Grid.Row="2" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />

                            <Label Grid.Row="3" Grid.Column="0" Content="{x:Static resources:Resource.SensorsControl_Fan_Title}" Style="{StaticResource StatLabelStyle}" />
                            <ProgressBar x:Name="_gpuFanSpeedBar" Grid.Row="3" Grid.Column="1" Margin="8,0,4,0" Maximum="100" Height="6">
                                <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                            </ProgressBar>
                            <Label x:Name="_gpuFanSpeedLabel" Grid.Row="3" Grid.Column="2" Content="" Style="{StaticResource StatValueStyle}" />
                        </Grid>

                        <StackPanel x:Name="_gpuDetailsPanel" AutomationProperties.AutomationId="DashboardSensorsGpuDetailsPanel" Visibility="Collapsed" Margin="0,16,0,0">
                            <Border x:Name="_gpuChartPanel" AutomationProperties.AutomationId="DashboardSensorsGpuChart" Style="{StaticResource SensorChartCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Canvas x:Name="_gpuChartCanvas" Grid.Row="0" Height="76" ClipToBounds="True">
                                        <Polygon x:Name="_gpuUtilizationArea" Fill="{DynamicResource SystemAccentColorBrush}" Opacity="0.14" />
                                        <Polygon x:Name="_gpuClockArea" Fill="{DynamicResource SystemFillColorSuccessBrush}" Opacity="0.10" />
                                        <Polygon x:Name="_gpuTemperatureArea" Fill="{DynamicResource SystemFillColorCautionBrush}" Opacity="0.12" />
                                        <Line X1="0" Y1="14" X2="420" Y2="14" Stroke="{DynamicResource CardStrokeColorDefaultBrush}" StrokeThickness="1" Opacity="0.55" />
                                        <Line X1="0" Y1="38" X2="420" Y2="38" Stroke="{DynamicResource CardStrokeColorDefaultBrush}" StrokeThickness="1" Opacity="0.35" />
                                        <Line X1="0" Y1="62" X2="420" Y2="62" Stroke="{DynamicResource CardStrokeColorDefaultBrush}" StrokeThickness="1" Opacity="0.55" />
                                        <Polyline x:Name="_gpuUtilizationSparkline" Stroke="{DynamicResource SystemAccentColorBrush}" StrokeThickness="2" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                        <Polyline x:Name="_gpuClockSparkline" Stroke="{DynamicResource SystemFillColorSuccessBrush}" StrokeThickness="2" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                        <Polyline x:Name="_gpuTemperatureSparkline" Stroke="{DynamicResource SystemFillColorCautionBrush}" StrokeThickness="2" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                    </Canvas>
                                    <UniformGrid Grid.Row="1" Columns="3" Margin="0,8,0,0">
                                        <StackPanel Margin="0,0,8,0">
                                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_Utilization_Title}" Style="{StaticResource SensorChartCaptionTextStyle}" />
                                            <TextBlock x:Name="_gpuChartUtilizationText" Text="-" Style="{StaticResource SensorChartValueTextStyle}" />
                                        </StackPanel>
                                        <StackPanel Margin="4,0,4,0">
                                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_CoreClock_Title}" Style="{StaticResource SensorChartCaptionTextStyle}" />
                                            <TextBlock x:Name="_gpuChartClockText" Text="-" Style="{StaticResource SensorChartValueTextStyle}" />
                                        </StackPanel>
                                        <StackPanel Margin="8,0,0,0">
                                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource SensorChartCaptionTextStyle}" />
                                            <TextBlock x:Name="_gpuChartTemperatureText" Text="-" Style="{StaticResource SensorChartValueTextStyle}" />
                                        </StackPanel>
                                    </UniformGrid>
                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                                        <Rectangle Width="8" Height="8" RadiusX="4" RadiusY="4" Fill="{DynamicResource SystemAccentColorBrush}" VerticalAlignment="Center" />
                                        <TextBlock Text="{x:Static resources:Resource.SensorsControl_Utilization_Title}" Style="{StaticResource SensorChartLegendTextStyle}" />
                                        <Rectangle Width="8" Height="8" RadiusX="4" RadiusY="4" Fill="{DynamicResource SystemFillColorSuccessBrush}" VerticalAlignment="Center" />
                                        <TextBlock Text="{x:Static resources:Resource.SensorsControl_CoreClock_Title}" Style="{StaticResource SensorChartLegendTextStyle}" />
                                        <Rectangle Width="8" Height="8" RadiusX="4" RadiusY="4" Fill="{DynamicResource SystemFillColorCautionBrush}" VerticalAlignment="Center" />
                                        <TextBlock Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource SensorChartLegendTextStyle}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <TextBlock Text="{x:Static resources:Resource.SensorsControl_MemoryClock_Title}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ProgressBar x:Name="_gpuMemoryClockBar" Grid.Column="0" Margin="0,0,8,0" Maximum="100" Height="6">
                                    <i:Interaction.Behaviors><behaviors:ProgressBarAnimateBehavior /></i:Interaction.Behaviors>
                                </ProgressBar>
                                <TextBlock x:Name="_gpuMemoryClockText" Grid.Column="1" Text="-" />
                            </Grid>

                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                    <TextBlock x:Name="_gpuVramUsageTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_gpuVramUsage" Text="" Margin="0,0,0,8" />

                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_PowerUsage}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_gpuWattage" Text="" Margin="0,0,0,8" />

                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_Voltage}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_gpuVoltage" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_gpuPcieThroughputTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_gpuPcieThroughput" Text="" Margin="0,0,0,8" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                    <TextBlock x:Name="_gpuVramTemperatureTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_gpuVramTemperature" Text="" Margin="0,0,0,8" />

                                    <TextBlock x:Name="_gpuHotSpotTemperatureTitle" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_gpuHotSpotTemperature" Text="" Margin="0,0,0,8" />

                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_gpuTempRange" Text="" Margin="0,0,0,8" />

                                    <TextBlock Text="{x:Static resources:Resource.SensorsControl_VoltageRange}" Style="{StaticResource AppCaptionTextBlockStyle}" />
                                    <TextBlock x:Name="_gpuVoltageRange" Text="" Margin="0,0,0,8" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </wpfui:CardControl.Header>
    </wpfui:CardControl>
</UserControl>
