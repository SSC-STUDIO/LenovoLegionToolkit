<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Settings.PluginSettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:LenovoLegionToolkit.WPF.Controls"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{x:Static resources:Resource.PluginSettingsWindow_Title}"
    Width="600"
    Height="500"
    MinWidth="500"
    MinHeight="400"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    ResizeMode="CanResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            Grid.Row="0"
            CanMaximize="true"
            UseSnapLayout="true" />

        <Grid
            Grid.Row="0"
            Height="24"
            Margin="12,8,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                x:Name="_titleTextBlock"
                VerticalAlignment="Center"
                FontSize="12"
                IsHitTestVisible="False"
                Text="{x:Static resources:Resource.PluginSettingsWindow_Title}" />
        </Grid>

        <ScrollViewer
            Grid.Row="1"
            Margin="24,0,24,24"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock
                    x:Name="_pluginNameTextBlock"
                    Margin="0,0,0,8"
                    FontSize="20"
                    FontWeight="Medium"
                    Text="{x:Static resources:Resource.PluginSettingsWindow_PluginName}" />
                
                <TextBlock
                    x:Name="_pluginDescriptionTextBlock"
                    Margin="0,0,0,16"
                    FontSize="14"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap"
                    Text="{x:Static resources:Resource.PluginSettingsWindow_PluginDescription}" />
                
                <!-- Plugin Settings Page Container -->
                <Border
                    x:Name="_pluginSettingsContainer"
                    Margin="0,0,0,16"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Visibility="Collapsed">
                    <Frame
                        x:Name="_pluginSettingsFrame"
                        NavigationUIVisibility="Hidden"
                        BorderThickness="0" />
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border
            Grid.Row="2"
            Padding="12,8,12,8"
            Background="{DynamicResource ControlFillColorDefaultBrush}"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <wpfui:Button
                    Grid.Column="1"
                    Content="{x:Static resources:Resource.PluginSettingsWindow_Close}"
                    Click="CloseButton_Click" />
            </Grid>
        </Border>
    </Grid>
</local:BaseWindow>
