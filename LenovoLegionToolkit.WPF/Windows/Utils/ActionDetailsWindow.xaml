<wpfui:UiWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Utils.ActionDetailsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    Title="{x:Static resources:Resource.ActionDetailsWindow_Title}"
    Height="600"
    Width="800"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize"
    Background="{DynamicResource ApplicationBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Margin="20,20,20,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Action title -->
                <TextBlock
                    x:Name="_titleTextBlock"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Margin="0,0,0,8"
                    TextWrapping="Wrap" />

                <!-- Action description -->
                <TextBlock
                    x:Name="_descriptionTextBlock"
                    FontSize="14"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Margin="0,0,0,20"
                    TextWrapping="Wrap" />

                <!-- Technical implementation details -->
                <wpfui:CardControl Margin="0,0,0,16">
                    <wpfui:CardControl.Header>
                        <TextBlock
                            FontSize="16"
                            FontWeight="Medium"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="{x:Static resources:Resource.ActionDetailsWindow_TechnicalDetails}" />
                    </wpfui:CardControl.Header>
                    <StackPanel Margin="0,12,0,0">
                        <!-- Implementation type -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                FontWeight="Medium"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                Text="{x:Static resources:Resource.ActionDetailsWindow_ImplementationType}" />
                            <TextBlock
                                x:Name="_implementationTypeTextBlock"
                                Grid.Column="1"
                                Margin="8,0,0,0"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                TextWrapping="Wrap" />
                        </Grid>

                        <!-- Detailed information -->
                        <Border
                            x:Name="_detailsBorder"
                            Padding="12"
                            Background="{DynamicResource ControlFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4">
                            <ScrollViewer
                                MaxHeight="300"
                                VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="_detailsStackPanel" />
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </wpfui:CardControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Close button -->
        <Grid Grid.Row="1" Margin="20" HorizontalAlignment="Right">
            <wpfui:Button
                Content="{x:Static resources:Resource.ActionDetailsWindow_Close_Button}"
                Appearance="Primary"
                Click="CloseButton_Click"
                MinWidth="100" />
        </Grid>
    </Grid>
</wpfui:UiWindow>


