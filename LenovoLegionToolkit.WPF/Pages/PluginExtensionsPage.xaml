<wpfui:UiPage
    x:Class="LenovoLegionToolkit.WPF.Pages.PluginExtensionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="PluginExtensionsPageRoot"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    Scrollable="True">
    
    <wpfui:UiPage.Resources>
        <!-- Tool Card Button Style (copied from ToolsPage) -->
        <Style x:Key="ToolCardButtonStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Margin" Value="0,0,12,12" />
            <Setter Property="Width" Value="110" />
            <Setter Property="MinHeight" Value="110" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect
                        BlurRadius="8"
                        ShadowDepth="2"
                        Opacity="0.1"
                        Color="Black" />
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform />
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}" />
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform Y="-2" />
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect
                                BlurRadius="12"
                                ShadowDepth="4"
                                Opacity="0.15"
                                Color="Black" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </wpfui:UiPage.Resources>

    <Grid Margin="0,0,16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,16,0,24">
            <TextBlock
                x:Name="_titleTextBlock"
                Focusable="True"
                FontSize="24"
                FontWeight="Medium"
                Text="{x:Static resources:Resource.PluginExtensionsPage_Title}" />
            <TextBlock
                x:Name="_descriptionTextBlock"
                Margin="0,8,0,0"
                Focusable="True"
                Text="{x:Static resources:Resource.PluginExtensionsPage_Description}" />
        </StackPanel>

        <!-- Search and Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <wpfui:TextBox
                    x:Name="_searchTextBox"
                    Grid.Column="0"
                    Icon="Search24"
                    PlaceholderText="{x:Static resources:Resource.PluginExtensionsPage_SearchPlaceholder}"
                    TextChanged="SearchTextBox_TextChanged" />
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0">
                <ComboBox
                    x:Name="_filterComboBox"
                    MinWidth="150"
                    SelectionChanged="FilterComboBox_SelectionChanged">
                    <ComboBoxItem Content="{x:Static resources:Resource.PluginExtensionsPage_FilterAll}" Tag="All" IsSelected="True" />
                    <ComboBoxItem Content="{x:Static resources:Resource.PluginExtensionsPage_FilterInstalled}" Tag="Installed" />
                    <ComboBoxItem Content="{x:Static resources:Resource.PluginExtensionsPage_FilterNotInstalled}" Tag="NotInstalled" />
                </ComboBox>
                
                <wpfui:Button
                    x:Name="_refreshButton"
                    Icon="ArrowClockwise24"
                    Appearance="Secondary"
                    Margin="8,0,0,0"
                    Click="RefreshButton_Click"
                    ToolTip="刷新插件列表" />
            </StackPanel>
        </Grid>

        <!-- Results Count -->
        <TextBlock
            x:Name="_resultsCountTextBlock"
            Grid.Row="2"
            Margin="0,0,0,12"
            FontSize="12"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Visibility="Collapsed" />

        <!-- Plugins List -->
        <Grid Grid.Row="3">
            <ScrollViewer
                x:Name="_pluginsScrollViewer"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled"
                Padding="0,0,0,24"
                ClipToBounds="False">
                <ItemsControl x:Name="_pluginsItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="0,2,0,0" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <!-- No Plugins Message -->
            <StackPanel
                x:Name="_noPluginsMessage"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Visibility="Visible"
                Margin="24">
                <wpfui:SymbolIcon
                    Symbol="Archive24"
                    FontSize="48"
                    Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,16" />
                <TextBlock
                    Text="{x:Static resources:Resource.PluginExtensionsPage_NoPluginsAvailable}"
                    FontSize="16"
                    FontWeight="Medium"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    HorizontalAlignment="Center"
                    TextAlignment="Center" />
                <TextBlock
                    Text="{x:Static resources:Resource.PluginExtensionsPage_EmptyStoreMessage}"
                    FontSize="14"
                    Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                    HorizontalAlignment="Center"
                    TextAlignment="Center"
                    Margin="0,8,0,0" />
            </StackPanel>
        </Grid>

        <!-- Plugin Details Panel -->
        <Border
            Grid.Row="2"
            x:Name="PluginDetailsPanel"
            Margin="0,16,0,0"
            Padding="16,12"
            Background="{DynamicResource ControlFillColorSecondaryBrush}"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0"
            CornerRadius="0"
            Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <!-- Icon -->
                <Border
                    Grid.Column="0"
                    Width="48"
                    Height="48"
                    Margin="0,0,16,0"
                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                    CornerRadius="8"
                    BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1">
                    <wpfui:SymbolIcon
                        x:Name="PluginDetailsIcon"
                        Symbol="Desktop24"
                        FontSize="24"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                </Border>
                
                <!-- Details -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0">
                        <TextBlock
                            x:Name="PluginDetailsName"
                            FontSize="14"
                            FontWeight="Medium"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Margin="0,0,0,2" />
                    <TextBlock
                        x:Name="PluginDetailsDescription"
                        FontSize="12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        TextWrapping="Wrap"
                        MaxHeight="100"
                        TextTrimming="CharacterEllipsis"
                        Margin="0,0,0,8" />
                    </StackPanel>
                    
                    <!-- Language Selection -->
                    <StackPanel 
                        Grid.Row="1"
                        Orientation="Horizontal"
                        Margin="0,0,0,8">
                        <TextBlock
                            Text="{x:Static resources:Resource.PluginExtensionsPage_Language}"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            VerticalAlignment="Center"
                            Margin="0,0,8,0" />
                        <ComboBox
                            x:Name="PluginLanguageComboBox"
                            MinWidth="150"
                            SelectionChanged="PluginLanguageComboBox_SelectionChanged"
                            Visibility="Collapsed" />
                    </StackPanel>
                    
                    <!-- Action Buttons -->
                    <StackPanel 
                        Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                        <wpfui:Button
                            x:Name="PluginInstallButton"
                            Content="{x:Static resources:Resource.PluginExtensionsPage_InstallPlugin}"
                            Click="PluginInstallButton_Click"
                            Margin="0,0,8,0"
                            Visibility="Collapsed" />
                        <wpfui:Button
                            x:Name="PluginUninstallButton"
                            Content="{x:Static resources:Resource.PluginExtensionsPage_Uninstall}"
                            Click="PluginUninstallButton_Click"
                            Margin="0,0,8,0"
                            Visibility="Collapsed" />
                        <wpfui:Button
                            x:Name="PluginConfigureButton"
                            Content="{x:Static resources:Resource.PluginExtensionsPage_Configure}"
                            Click="PluginConfigureButton_Click"
                            Margin="0,0,8,0"
                            Visibility="Collapsed" />
                        <wpfui:Button
                            x:Name="PluginOpenButton"
                            Content="{x:Static resources:Resource.PluginExtensionsPage_Open}"
                            Click="PluginOpenButton_Click"
                            Margin="0,0,8,0"
                            Visibility="Collapsed" />
                        <wpfui:Button
                            x:Name="PluginPermanentlyDeleteButton"
                            Content="{x:Static resources:Resource.PluginExtensionsPage_PermanentlyDelete}"
                            Click="PluginPermanentlyDeleteButton_Click"
                            Appearance="Danger"
                            Margin="0,0,8,0"
                            Visibility="Collapsed" />
                    </StackPanel>
                    
                    <!-- Download Progress -->
                    <StackPanel 
                        Grid.Row="4"
                        Margin="0,8,0,0"
                        Visibility="Collapsed">
                        <TextBlock
                            Text="{x:Static resources:Resource.PluginExtensionsPage_Downloading}"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Margin="0,0,4,0" />
                        <ProgressBar
                            x:Name="DownloadProgressBar"
                            Height="4"
                            Margin="0,0,8,0" />
                        <TextBlock
                            x:Name="DownloadProgressText"
                            FontSize="10"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            HorizontalAlignment="Right"
                            Margin="0,0,8,0" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</wpfui:UiPage>
