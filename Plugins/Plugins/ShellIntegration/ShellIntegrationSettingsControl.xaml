<UserControl x:Class="LenovoLegionToolkit.Plugins.ShellIntegration.ShellIntegrationSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:text="clr-namespace:LenovoLegionToolkit.Plugins.ShellIntegration"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="420"
             d:DesignWidth="760">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{x:Static text:ShellIntegrationText.Subtitle}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16" />

        <TextBlock Grid.Row="1"
                   x:Name="_statusTextBlock"
                   AutomationProperties.AutomationId="StatusText"
                   TextWrapping="Wrap"
                   Foreground="#FF2E7D32" />

        <WrapPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="{x:Static text:ShellIntegrationText.EnableButton}"
                    AutomationProperties.AutomationId="EnableButton"
                    Width="90"
                    Margin="0,0,8,8"
                    Click="EnableButton_Click" />
            <Button Content="{x:Static text:ShellIntegrationText.DisableButton}"
                    AutomationProperties.AutomationId="DisableButton"
                    Width="90"
                    Margin="0,0,8,8"
                    Click="DisableButton_Click" />
            <Button Content="{x:Static text:ShellIntegrationText.OpenStyleSettingsButton}"
                    AutomationProperties.AutomationId="OpenStyleSettingsButton"
                    Width="160"
                    Margin="0,0,8,8"
                    Click="OpenStyleButton_Click" />
            <Button Content="{x:Static text:ShellIntegrationText.OpenShellFolderButton}"
                    AutomationProperties.AutomationId="OpenShellFolderButton"
                    Width="160"
                    Margin="0,0,8,8"
                    Click="OpenShellFolderButton_Click" />
            <Button Content="{x:Static text:ShellIntegrationText.OpenConfigButton}"
                    AutomationProperties.AutomationId="OpenConfigButton"
                    Width="160"
                    Margin="0,0,8,8"
                    Click="OpenConfigButton_Click" />
        </WrapPanel>

        <TextBlock Grid.Row="3"
                   Text="{x:Static text:ShellIntegrationText.OptimizationHint}"
                   Margin="0,12,0,0"
                   Foreground="#FF6A6A6A" />
    </Grid>

</UserControl>
