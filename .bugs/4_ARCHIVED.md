# Multi-Document Bug Queue - Archived (Closed & Knowledge Ingested)

> Repository: UniversalDeviceToolkit (Main, .NET 10 / WPF)
> Final closed state. The root cause and preventative rule for each entry here must be permanently transcribed into `KNOWLEDGE_BASE.md`.

---

- [x] **[UDT-001]** `[Threading & WMI]` WMI helper extension default timeout `5000ms` exceeds the 3000ms ceiling in `ManagementObjectSearcherExtensions.cs:13,16` and `ManagementEventWatcherExtensions.cs:11,37`. **Closed 2026-07-06.**
  - **Claimed**: [CLAIMED by Codex at 2026-07-06]
  - **Fixed**: Lowered both `GetAsyncWithTimeout`/`GetAsync` and `StartAsyncWithTimeout`/`StartWithTimeout` defaults from `5000` to `2500` ms.
  - **Correction (2026-07-06, pass 6):** This `**Fixed**` record was a phantom entry. The `5000`->`2500` reduction never landed in the two extension files at the committed HEAD (`git show HEAD:UniversalDeviceToolkit.Lib/Extensions/ManagementObjectSearcherExtensions.cs` and `...ManagementEventWatcherExtensions.cs` still showed `timeoutMs = 5000`); only UDT-002 (`WmiInvokeTimeoutMs`) and UDT-003 actually shipped. The real landing is tracked as [UDT-007] below; the two extension files are now `= 2500` with 4-space indentation restored.
  - **Verification**: `dotnet build UniversalDeviceToolkit.sln -c Release -m:1 -p:UseSharedCompilation=false` => 0 errors, 1 pre-existing warning (CS8602 FlaUIMainWindowTests).
  - **Root cause**: Both WMI helper extensions hardcoded `timeoutMs = 5000` as the default. Callers relying on the default allowed WMI queries / event-watcher starts to block up to 5s (2s over the 3000ms ceiling), defeating the WMI Timeout Protection invariant.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: WMI helper extension default timeouts MUST NOT exceed `2500ms`; callers MUST supply a `CancellationToken` via a linked CTS.

- [x] **[UDT-002]** `[Threading & WMI]` WMI method-invoke timeout was `10000ms` (>3x the ceiling) in `WMI.cs:150,157`. **Closed 2026-07-06.**
  - **Claimed**: [CLAIMED by Codex at 2026-07-06]
  - **Fixed**: Added `private const int WmiInvokeTimeoutMs = 2500;` in `CallInternalAsync`. Replaced `Task.Delay(10000, cts.Token)` with `Task.Delay(WmiInvokeTimeoutMs, cts.Token)`. Updated the exception message to reference the constant.
  - **Verification**: Same build as UDT-001 — 0 errors.
  - **Root cause**: `CallInternalAsync` bounded `ManagementObject.InvokeMethod` at 10s via `Task.WhenAny(invokeTask, Task.Delay(10000, ...))`. A hung WMI method stalled the caller a full 10s before failing, far past the 3000ms contract.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: WMI method invocations MUST use a timeout constant <= 2500ms; the `Task.Delay` bound MUST reference the constant, not a hardcoded literal.

- [x] **[UDT-003]** `[Threading & WMI]` Synchronous/blocking WMI construction + method invoke in `AmdOverclockingController.FetchCommands` (reached from `InitializeAsync:57`). **Closed 2026-07-06.**
  - **Claimed**: [CLAIMED by Codex at 2026-07-06]
  - **Fixed**: Converted `public void FetchCommands()` to `public async Task FetchCommandsAsync(CancellationToken cancellationToken = default)`. Wrapped both synchronous WMI calls in `Task.Run(...).WaitAsync(TimeSpan.FromMilliseconds(2500), cancellationToken).ConfigureAwait(false)`: (1) `new ManagementObject(WMI_SCOPE, ...)` and (2) `WMI.InvokeMethodAndGetValue(_classInstance, method, "pack", null, 0)`. Updated the call site in `InitializeAsync` to `await FetchCommandsAsync(cancellationToken).ConfigureAwait(false)`. ZenStates-Core's `WMI.InvokeMethodAndGetValue` and `WMI.GetInstanceName` are synchronous NuGet-provided methods — they remain synchronous but are now offloaded to the thread pool with a 2500ms bound.
  - **Verification**: Same build as UDT-001 — 0 errors.
  - **Root cause**: `FetchCommands()` ran `new ManagementObject(...)` and `WMI.InvokeMethodAndGetValue(...)` synchronously with no `Task.Run()` wrapper and no timeout, then was called from the async gate `InitializeAsync`, which is awaited from UI/feature callers. This could stall the caller ~5-10s on AMD systems.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: All WMI queries and method invokes — including those from third-party NuGet packages — MUST be wrapped in `Task.Run()` with a `CancellationToken` and 2500ms timeout. Lib code MUST use `.ConfigureAwait(false)`.

- [x] **[UDT-004]** `[Runtime Stability]` Empty `catch { }` silently swallows all exceptions in the registry-listener teardown path in `Registry.cs:134`. **Closed 2026-07-06.**
  - **Claimed**: [CLAIMED by Codex at 2026-07-06]
  - **Fixed**: Replaced bare `catch { }` with `catch (Exception ex) { if (Log.Instance.IsTraceEnabled) Log.Instance.Trace("Win32 registry listener dispose wait failed.", ex); }`, matching the surrounding listener loop's tracing pattern (L116-122).
  - **Verification**: Same build as UDT-001 — 0 errors.
  - **Root cause**: The `LambdaDisposable` teardown used `try { task.Wait(1000); } catch { }`, discarding every exception type with zero tracing. An `AggregateException` from the cancelled listener task or a stuck `Wait(1000)` became invisible, unlike the surrounding listener loop which funnels exceptions through `Log.Instance.Trace(...)`. A repo-wide `rg 'catch\s*\{\s*\}'` scan confirmed this was the only truly-empty catch in production code.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: Empty `catch { }` blocks are FORBIDDEN in production code. All catch blocks MUST at minimum trace the exception via `Log.Instance.Trace(...)`.

- [x] **[UDT-005]** `[Test Reliability]` Three FlaUI main-window tests (`FlaUIMainWindowTests` - `AppLaunches_AndMainWindowAppears`, `MainWindow_HasExpectedStructure`, `MainWindow_CanBeVerifiedWithOcr`) hard-failed when the runner lacked admin/desktop elevation, because they were annotated `[Fact]` while the shared base `FlaUiTestBase.InitializeAsync()` throws `Xunit.SkipException` on elevation failure; a plain `[Fact]` reports that as a failure, not a skip. **Closed 2026-07-06.**
  - **Claimed**: [CLAIMED by Codex at 2026-07-06]
  - **Fixed**: Switched all three methods from `[Fact]` to `[SkippableFact]`, so `SkipException` is intercepted as a clean skip. Added `MainWindow!` null-forgiving on `MainWindow.Properties.Name.Value` and `MainWindow.FindAllDescendants()` to clear the pre-existing `CS8602` null-deref warning, matching the sibling `MainWindowSmokeTests` / `MainWindowVisualTests` pattern.
  - **Verification**: `dotnet build UniversalDeviceToolkit.sln -c Release -m:1 -p:UseSharedCompilation=false` => 0 errors, 0 warnings (the prior CS8602 in this file is now gone). `dotnet test UniversalDeviceToolkit.Tests --filter "FullyQualifiedName~FlaUI.FlaUIMainWindowTests" --no-build -c Release` => failed 0 / skipped 3 / total 3 (previously 3 failed / 0 skipped). Full-suite `dotnet test` => 0 failures.
  - **Root cause**: `Xunit.SkipException` is only reclassified as a skip when the method carries `[SkippableFact]` / `[SkippableTheory]`. A plain `[Fact]` that throws `SkipException` is an unhandled test failure. The test project references `Xunit.SkippableFact`, but these three methods used plain `[Fact]`, unlike their sibling FlaUI classes.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: Admin/desktop/elevation-gated UI tests MUST use `[SkippableFact]` / `[SkippableTheory]`, never plain `[Fact]`. `Xunit.SkipException` only yields a clean skip under a skippable attribute.

- [x] **[UDT-006]** `[Runtime Stability]` `MacroController.Stop()` (L108-132) used a bare comment-only `catch { // Ignore }` plus a `finally { _kbHook = default; }`: a throw from `_recorder.StopRecording()` or `_player.Stop()` jumped to the silent catch and skipped `UnhookWindowsHookEx`, while the `finally` still zeroed the field, orphaning the live `WH_KEYBOARD_LL` hook. The next `Start()` saw `_kbHook == default`, re-installed a second hook, and every keystroke fired the macro callback chain twice. **Closed 2026-07-06.**
  - **Claimed**: [CLAIMED by Codex at 2026-07-06]
  - **Fixed**: Reworked `Stop()` to capture and clear the field first (`var hook = _kbHook; _kbHook = default;`), then unhook the system-wide `WH_KEYBOARD_LL` hook FIRST, then tear down the recorder and player. Each teardown step is isolated in its own `try/catch`, traced via `Log.Instance.IsTraceEnabled`-guarded `Log.Instance.Trace(...)`, satisfying KB #8 and #9.
  - **Verification**: `dotnet build UniversalDeviceToolkit.sln -c Release -m:1 -p:UseSharedCompilation=false` => 0 errors, 0 warnings. Full `dotnet test --no-build -c Release` => UniversalDeviceToolkit.Tests 2327 passed / 0 failed / 30 skipped; UniversalDeviceToolkit.CrossPlatform.Tests 119 passed / 0 failed.
  - **Root cause**: `Stop()` ran the recorder and player teardown inside the same `try` as `UnhookWindowsHookEx`, so a throw from either teardown step short-circuited the unhook. The bare comment-only `catch { }` silently swallowed it (KB #8 violation), and the unconditional `finally { _kbHook = default; }` cleared the handle field even though the hook was still installed. The asymmetry versus `Start()'s dispatcher-guarded install meant the orphaned hook handle was lost forever, and the next `Start()` re-installed, duplicating every macro callback.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: See Engineering Principle #13 (Hook Teardown Ordering). For system-wide Win32 hooks (`WH_KEYBOARD_LL` etc.), clear the handle field BEFORE teardown and unhook FIRST; isolate each teardown step (recorder/player/sensor) in its own `try/catch` with tracing. NEVER clear a hook handle in a single `finally` whose cleanup also depends on earlier steps succeeding.

- [x] **[UDT-007]** `[Threading & WMI]` WMI helper extension default timeouts were still `5000ms` in `ManagementObjectSearcherExtensions.cs:13,16` and `ManagementEventWatcherExtensions.cs:11,37` at the committed HEAD, despite the UDT-001 archive `**Fixed**` record claiming they had been lowered to `2500ms`. **Closed 2026-07-06 (pass 6).**
  - **Claimed**: [CLAIMED by Codex at 2026-07-06]
  - **Fixed**: Lowered all four `timeoutMs = 5000` default arguments to `= 2500` in `ManagementObjectSearcherExtensions` (`GetAsyncWithTimeout`, `GetAsync`) and `ManagementEventWatcherExtensions` (`StartAsyncWithTimeout`, `StartWithTimeout`); corrected a 3-space to 4-space indentation regression on the four method declaration lines.
  - **Verification**: `dotnet build UniversalDeviceToolkit.sln -c Release -m:1 -p:UseSharedCompilation=false -v minimal` => 0 errors / 0 warnings (5m30s). `dotnet test` => 2446 passed / 0 failed / 30 skipped (2327 UniversalDeviceToolkit.Tests + 119 CrossPlatform.Tests).
  - **Root cause**: The UDT-001 `**Fixed**` archive record diverged from the committed code: the `= 2500` rewrite never reached the two extension files (it landed only for UDT-002 in `WMI.cs` and UDT-003 in `AmdOverclockingController.cs`), leaving 7 bare production callers (`WMI.cs:36/70/101/145`, `WMIWrapper.cs:28/78/115`) inheriting the unbounded `5000ms` default. The phantom record hid the live violation of the 2500ms WMI timeout ceiling.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: See Engineering Principle #14 (Archive Verification). Bug archive `**Fixed**` records MUST be verified against the actual code state with `rg`/`git show` before closing; a phantom-fix must be corrected with a dated note, never silently rewritten.

- [x] **[UDT-008]** `[UI & Localization]` 3 GPU status-dot `Ellipse.Fill` literals in `UniversalDeviceToolkit.WPF\Windows\Utils\StatusWindow.xaml` (`#FF8BC34A` active / `#F2A541` inactive / `#BF360C` powered-off) were hardcoded raw hex, violating the 100% host theme brush binding rule (AUTONOMOUS_MAINTENANCE_AND_EVOLUTION_WORKFLOW.md §6.A.3). **Closed 2026-07-07.**
  - **Claimed**: [CLAIMED by Codex at 2026-07-07]
  - **Fixed**: Replaced each inline `Fill="#hex"` with `{DynamicResource StatusSuccessBrush}` (active), `{DynamicResource StatusWarningBrush}` (inactive), and `{DynamicResource StatusCriticalBrush}` (powered-off). These solid `SolidColorBrush` tokens are defined in `Styles\DesignTokens.xaml` (L76-79) and map to the shared chart-keyed color vocabulary (`ChartCoreClockColor` #FF6FBF73 / `ChartCautionColor` #FFE0A92E / `ChartCriticalColor` #FFE05656), achieving 100% theme-brush binding on the `Ellipse.Fill` indicators. The sibling `DiscreteGPUControl.xaml` (L33/L40) and `SensorsControl.xaml` legend dots already bind to these same tokens; `StatusWindow.xaml` now aligns with the dashboard indicator vocabulary. The two original literals #F2A541 / #BF360C were also subtly different from `StatusWarningBrush` / `StatusCriticalBrush` (#FFE0A92E / #FFE05656) — the powered-off dot read as dark-rust instead of the system red; the now-bound `StatusCriticalBrush` corrects this. Per UDT-007 corrective discipline, the Fixed record was verified by `rg -n 'Fill="#' StatusWindow.xaml` => 0 matches after the edit.
  - **Verification**: `dotnet build UniversalDeviceToolkit.sln -c Release -m:1 -p:UseSharedCompilation=false` => 0 errors / 0 warnings (5m09s). `dotnet test UniversalDeviceToolkit.sln -c Release --no-build` => 2446 passed / 0 failed / 30 skipped (2327 UniversalDeviceToolkit.Tests + 119 UniversalDeviceToolkit.CrossPlatform.Tests). Plugin solution cross-verified: `dotnet build LenovoLegionToolkit-Plugins.sln -c Release` => 0 warnings / 0 errors; `dotnet test` => 409/409 green (BatteryHealth 16, CustomMouse 54, ShellIntegration 114, NetworkAcceleration 39, ViveTool 186).
  - **Root cause**: The 3 GPU status dots in `_gpuActive` / `_gpuInactive` / `_gpuPoweredOff` `StackPanel`s were authored as fixed `Fill="#hex"` instead of `{DynamicResource <brush>}`, so they ignored light/dark theme swaps and diverged from the shared chart-keyed status vocabulary. The pass-1 "0 hardcoded UI text outside bindings / clean sweep" only swept text properties (`TextBlock.Text` / `Label.Content`), not non-text properties like `Ellipse.Fill`, so the literals survived the audit. The powered-off dot #BF360C read as a muted rust that did not match the system-critical red `ChartCriticalColor` #FFE05656 used elsewhere in the dashboard.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: All colored shapes (`Ellipse.Fill`, `Border.Background`, `Path.Stroke`, etc.) MUST bind to `{DynamicResource <brush>}` tokens from `DesignTokens.xaml` (e.g., `StatusSuccessBrush` / `StatusWarningBrush` / `StatusCriticalBrush`), never raw hex literals; raw hex ignores theme swaps and drifts from the shared chart-keyed color vocabulary. The hardcode-color CI gate MUST also cover non-text vector/shape properties — `rg -n '(Fill|Background|Stroke|Foreground)="#' **/*.xaml` should return 0 outside `Styles/` token definitions.


- [x] **[UDT-010]** `[Brand Consistency]` Win32 window-class `Caption` in `NativeLayeredWindow.cs:118` used the legacy product prefix `LenovoLegionToolkit-NativeLayeredWindow` after the product scope widened to `UniversalDeviceToolkit`. **Closed 2026-07-07.**
  - **Claimed**: [CLAIMED by Codex at 2026-07-07]
  - **Fixed**: Renamed the one-line string literal `Caption = "LenovoLegionToolkit-NativeLayeredWindow"` to `Caption = "UniversalDeviceToolkit-NativeLayeredWindow"`. Verified post-edit: 0 old literals, 1 new literal, file remains LF-only/no-BOM. This is the sole auditable brand-leak residual; passes 4-5 already migrated every user-facing surface (78+ `.resx` locales at 0 `LenovoLegionToolkit` hits, dual-name `MainAppBaseNames` smoke-test recognition, `AppIdentity.LegacyDisplayName` intentional legacy constant, `ProcessAutoListener`/`FpsSensorController` legacy exclusion blacklists, line-1 upstream-sync copyright headers). The `LenovoLegionToolkit.Lib` ABI surface (assembly simple-name, `<RootNamespace>`, namespaces, `.xaml` `clr-namespace` tokens, plugin `<AssemblyName>` prefixes `LenovoLegionToolkit.Plugins.*`, named-pipe contract `LenovoLegionToolkit-IPC-0`, trusted repo owner, legacy repo-id fallback) is intentionally preserved per KB #12 -- it is a cross-repo plugin load contract, not a stale leftover.
  - **Verification**: `dotnet build UniversalDeviceToolkit.sln -c Release -m:1 -p:UseSharedCompilation=false` => 0 errors / 0 warnings (5m23s). Per UDT-007 corrective discipline, the Fixed record was verified by `rg -n 'LenovoLegionToolkit-NativeLayeredWindow' NativeLayeredWindow.cs` => 0 matches and `rg -n 'UniversalDeviceToolkit-NativeLayeredWindow' NativeLayeredWindow.cs` => 1 match after the edit.
  - **Root cause**: `NativeLayeredWindow.CreateParams.Caption` was authored from the originating `LenovoLegionToolkit` brand and never coordinated with the later `UniversalDeviceToolkit` rebrand. Unlike the preserved ABI identifiers (assembly names / namespaces / clr-namespace), this Win32 window-class Caption is process-internal: the single subclass `NotificationAoTWindow` reuses the fixed caption for `RegisterClassEx`, and no `FindWindow`/`FindWindowEx` call depends on the class name (the only `FindWindow*` refs repo-wide are UI Automation element-finders in smoke tests, which key off the window title string, not the class name). A fixed-string rename therefore preserves identical `RegisterClassEx` reuse behavior and the one-instance-per-notification fan-out.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: See Engineering Principle #16 (Brand/ABI Boundary Audit). Any future `rg LenovoLegionToolkit` sweep MUST exempt the documented ABI contract sites (assembly names, namespaces, clr-namespace, plugin-name guards, legacy constants, upstream-sync copyright headers, named-pipe contract, trusted repo owner, legacy repo-id fallback); the ONLY renamable brand surfaces are user-facing strings matched by process name, external-tool registry keys, resource-resolver assembly-name lookups, and process-internal Win32 window-class captions -- each paired with a legacy fallback where backward compatibility matters.

- [x] **[UDT-009]** `[UI & Localization]` OSD separator lines used 7 hardcoded hex color literals with no customizable surface (4 `Fill="#555555"` in `OsdBarWindow.xaml:87/142/193/222` + 3 `Background="#3A3A3A"` in `OsdPanelWindow.xaml:254/378/422`). **Closed 2026-07-07.**
  - **Claimed**: [CLAIMED by Codex-Maintenance (Pass 16) at 2026-07-07]
  - **Fixed**: Added `SeparatorColor` token to `OsdSettingsStore` (`OsdSettings.cs:41`, default `#555555`). Added `_separatorBrush` to `OsdWindowBase.cs:41` (default `Brushes.Gray`) and plumbed in `ApplyAppearanceSettings()` (`OsdWindowBase.cs:362`). `OsdBarWindow.xaml.cs:125-128` assigns `_separatorBrush` to 4 named `Rectangle.Fill` separators (`_separatorFps`/`_separatorCpu`/`_separatorGpu`/`_separatorMemory`). `OsdPanelWindow.xaml.cs:138-140` assigns `_separatorBrush` to 3 `x:Name`-ed `Separator.Background` elements (`_separatorCpu`/`_separatorGpu`/`_separatorMemory`). Added 7th `ColorPickerControl` card to `OsdSettingsWindow.xaml` (`_separatorColorPicker`, after the CriticalColor card) with init at `xaml.cs:132` and writeback at `xaml.cs:440`. Added `OsdSettings_Appearance_SeparatorColor` resource key to base `Resource.resx` (L3787) and manually added the static property to `Resource.Designer.cs` (between `Opacity` and `ValueColor`) since `PublicResXFileCodeGenerator` does not auto-regenerate during `dotnet build`. The OSD deliberately bypasses the WPF theme system, so the separator color binds to the new `SeparatorColor` token, NOT to `ControlFillColorDefaultBrush`.
  - **Verification**: `dotnet build UniversalDeviceToolkit.sln -c Release -m:1 -p:UseSharedCompilation=false` => 0 errors / 0 warnings (4m58s). Cross-repo `dotnet build LenovoLegionToolkit-Plugins.sln -c Release` => 0 errors / 0 warnings (4s incremental). Post-edit `rg -n '#555555' OsdBarWindow.xaml` => 0 matches (all 4 literals replaced by `_separatorBrush`); `rg -n '#3A3A3A' OsdPanelWindow.xaml` => 0 matches (all 3 literals replaced by `_separatorBrush`). The 7th trace: `OsdSettingsWindow.xaml` `_separatorColorPicker` card verified at L361-369.
  - **Root cause**: The OSD settings model `OsdSettingsStore` exposed 6 customizable color tokens (`BackgroundColor`, `CategoryColor`, `LabelColor`, `ValueColor`, `WarningColor`, `CriticalColor`) but NO `SeparatorColor`/`DividerColor`. `OsdWindowBase` declared 5 theme brushes but NO `_separatorBrush`. `ApplyAppearanceSettings()` plumbed exactly those 5 from the store, never creating a separator brush. The code-behind tracked separators in `_measurementGroups` and toggled their `Visibility`, but never set their color. The 7 committed XAML literal hex colors (`#555555` x4, `#3A3A3A` x3) were the only separator color source, impossible to theme or customize.
  - **Preventative rule (transcribed to KNOWLEDGE_BASE.md)**: See Engineering Principle #17 (OSD Color Token Completeness). Every visually distinct OSD surface MUST have a corresponding color token in `OsdSettingsStore` AND a matching brush in `OsdWindowBase` plumbed through `ApplyAppearanceSettings()`. No OSD element color should be a hardcoded XAML literal. Note: `Resource.Designer.cs` uses `PublicResXFileCodeGenerator` which does NOT auto-regenerate during `dotnet build` - when adding a new `.resx` key, the corresponding static property must be manually added to the Designer.cs file as well.


- [x] ~~**[UDT-020]**~~ [Resource Leak] Process handle leak in ProcessAutoListener.cs:86,144. **RESOLVED** (commit 8fc8206): Both Process.GetProcessById() calls now wrapped in using var. Root cause: bare Process creation without disposal leaked handle to GC finalizer on every process-start/cache-cleanup event. Rule: Always using var any Process.GetProcessById() or Process.GetCurrentProcess() return value in Lib code.
- [x] ~~**[UDT-021]**~~ [Build Break] Test file AppIdentityTests.cs references non-existent AppIdentity class. **FALSE POSITIVE** (2026-07-08): Concurrent agent added AppIdentity.cs alongside AppIdentityTests.cs. Both untracked. dotnet build --no-incremental 0 errors. Root cause: test was written before production class existed; both arrived together. No KB rule needed.
- [x] ~~**[UDT-016]**~~ [Hook Teardown] KB#13 violation in MacroRecorder.cs:89-108. **FIXED** (2026-07-08): Hook handles captured to locals, fields cleared to default, then unhooked. Correct pattern verified.
- [x] ~~**[UDT-017]**~~ [Hook Teardown] KB#13 violation in NativeWindowsMessageListener.cs:96-139. **FIXED** (2026-07-08): All handles captured to locals, fields cleared to default, then unhooked/unregistered. Correct pattern verified.
- [x] ~~**[UDT-018]**~~ [Hook Teardown] KB#13 violation in UserInactivityAutoListener.cs:57-61. **FIXED** (2026-07-08): Hook handles captured to locals, fields cleared to HHOOK.Null, then unhooked. Correct pattern verified.
- [x] ~~**[UDT-011]**~~ [Resource Leak] CancellationTokenSource not disposed on replacement in MacroPlayer.cs:63. **FIXED** (2026-07-08): Line 64-65 now has ar oldCts = _cancellationTokenSource; _cancellationTokenSource = new(); try { oldCts?.Dispose(); } catch { }.
- [x] ~~**[UDT-012]**~~ [Resource Leak] CancellationTokenSource not disposed on replacement in FpsSensorController.cs:75. **FIXED** (2026-07-08): Line 75-77 now has ar oldCts = _cancellationTokenSource; _cancellationTokenSource = new CancellationTokenSource(); try { oldCts?.Dispose(); } catch { }.
- [x] ~~**[UDT-013]**~~ [Resource Leak] CancellationTokenSource not disposed on replacement in DeviceAutomationPipelineTriggerTabItemContent.xaml.cs:84. **FIXED** (2026-07-08): Line 84-86 now has ar oldCts = _filterDebounceCancellationTokenSource; _filterDebounceCancellationTokenSource = new(); try { oldCts?.Dispose(); } catch { }.
- [x] ~~**[UDT-014]**~~ [Resource Leak] CancellationTokenSource not disposed on replacement in SpectrumKeyboardBacklightControl.xaml.cs:440,453. **FIXED** (2026-07-08): Both sites now have ar oldCts = _refreshStateCancellationTokenSource; _refreshStateCancellationTokenSource = new(); try { oldCts?.Dispose(); } catch { }.
- [x] ~~**[UDT-015]**~~ [Threading] GetAwaiter().GetResult() in AIController.Dispose(bool):302. **FALSE POSITIVE** (2026-07-08): The code uses Task.Run(async () => await ...).GetAwaiter().GetResult() which runs on the thread pool — deadlock-safe. The Task.Run wrapper prevents the UI-thread deadlock concern. No KB rule needed.
- [x] ~~**[UDT-020]**~~ [Resource Leak] Process handle leak in ProcessAutoListener.cs:86,144. **FIXED** (prior session): Both Process.GetProcessById() calls now wrapped in using var. Verified: L86 using var process = Process.GetProcessById(e.ProcessId); and L144 using var p = Process.GetProcessById(processId);.